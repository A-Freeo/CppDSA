#include "Queue.hpp"
#include "QueueController.hpp"
#include <iostream>

int main(){
    std::cout << "========== Queue demo ==========\n";

    Queue<int> q;
    // TODO: once enqueue/dequeue/etc. are filled in, demo them here, e.g.:
    // for(int x : {1, 2, 3, 4}) q.enqueue(x);
    // QueueController::dump(q);
    // std::cout << "dequeue() = " << q.dequeue() << "\n";
    // QueueController::dump(QueueController::reverse(q));

    return 0;
}
